[package]
name = "bazel-remote-apis"
version = "0.1.0"
edition = "2021"
publish = false

[features]
codegen = []

[dependencies]
tonic = "0.8.0"
prost = "0.11.0"
prost-types = "0.11.0"

[build-dependencies]
tonic-build = "0.8.0"
